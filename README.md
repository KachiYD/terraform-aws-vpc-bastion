# terraform-aws-vpc-bastion
Terraform build of a multi-AZ AWS VPC with a single NAT gateway shared by both provate subnets - a deliberate cost tradeoff over the one-per-AZ pattern, a bastion host, and Python web apps on private EC2 instances
